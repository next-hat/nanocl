{% if domain %}# vhost {{ key }}
acl host_{{ key }} hdr(host),field(1,:) -i {{ domain }}
{% if hsts %}http-response set-header Strict-Transport-Security "max-age={{ hsts.MaxAge }}{% if hsts.IncludeSubDomains %}; includeSubDomains{% endif %}{% if hsts.Preload %}; preload{% endif %}"{% if hsts.Always %} always{% endif %} if { ssl_fc } host_{{ key }}
{% endif %}{% endif %}
{% for location in locations %}acl {{ key }}_loc{{ forloop.index }} path_beg {{ location.path }}
use_backend bk_{{ location.upstream_key | split: "//" | last }} if host_{{ key }} {{ key }}_loc{{ forloop.index }}
{% endfor %}
{% if locations.size > 0 %}use_backend bk_{{ locations[0].upstream_key | split: "//" | last }} if host_{{ key }}{% endif %}
