Kind: Cargo
ApiVersion: v0.7

Namespace: global

# See all options:
# https://docs.next-hat.com/references/nanocl/cargo
Cargoes:
  - Name: cargo-autoremove
    Container:
      Image: busybox:uclibc
      Cmd:
        - sleep
        - "20"
      HostConfig:
        AutoRemove: true
