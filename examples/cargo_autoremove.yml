ApiVersion: v0.16

Namespace: global

# See all options:
# https://docs.next-hat.com/references/nanocl/objects/cargo
Cargoes:
- Name: cargo-autoremove
  # Docker AutoRemove is intentionally unsupported for Nanocl-managed Cargoes.
  Containers:
  - Name: cargo-autoremove
    Image: busybox:uclibc
    Cmd:
      - sleep
      - "20"
